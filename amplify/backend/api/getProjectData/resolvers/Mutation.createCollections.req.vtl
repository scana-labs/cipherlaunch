#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCollectionsInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCollectionsInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Collections $colStr VALUES $valStr", "SELECT * FROM Collections WHERE collection_id='$ctx.args.createCollectionsInput.collection_id'"]
}