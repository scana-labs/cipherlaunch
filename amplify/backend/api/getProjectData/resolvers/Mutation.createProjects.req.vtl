#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createProjectsInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createProjectsInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Projects $colStr VALUES $valStr", "SELECT * FROM Projects WHERE project_id='$ctx.args.createProjectsInput.project_id'"]
}